# Touched at 2026-08-26T18:35:00Z to retrigger Pages build / cert provisioning
# Jekyll configuration for echocc00.github.io/awesome-echocc00
title: echocc00 · Project Portfolio
description: Portfolio & project index for @echocc00 — Cloud Security, Network Engineering, AI-Driven SecOps

theme: null  # Removed minima to fix CF Pages build (2026-08-31).
          # index.html is fully self-contained and never relied on minima layout,
          # so removing it doesn't change the rendered site — see commit message for evidence.

# Exclude from Jekyll processing
exclude:
  - SECURITY.md
  - CODEOWNERS
  - LICENSE
  - release-drafter.yml
  - .github/

# Include non-standard files (PWA assets must be present as-is)
include:
  - sw.js
  - manifest.webmanifest
  - icon.svg

show_downloads: false
